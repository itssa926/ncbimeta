% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_build.R
\name{build_tree_label}
\alias{build_tree_label}
\title{Build tree labels from metadata columns}
\usage{
build_tree_label(
  df,
  cols,
  sep = "|",
  na_token = "None",
  sanitize = TRUE,
  new_col = "label",
  compact_unknown_dates = TRUE,
  unknown_token = "??"
)
}
\arguments{
\item{df}{A data.frame or tibble containing metadata.}

\item{cols}{Columns to use in the label (tidyselect). Order matters.}

\item{sep}{Separator used to join fields (default "|").}

\item{na_token}{Token used when a field is missing/empty (default "None").}

\item{sanitize}{Logical; if TRUE, apply \code{make_tree_safe()} to each field before joining.}

\item{new_col}{Name of the output label column (default "label").}

\item{compact_unknown_dates}{Logical; if TRUE, compress date strings like
\verb{YYYY-??-??} -> \code{YYYY} and \verb{YYYY-MM-??} -> \code{YYYY-MM} before sanitization.}

\item{unknown_token}{Token used for unknown date components (default "??").}
}
\value{
The input data frame with an added label column.
}
\description{
Creates a single label column by pasting selected metadata columns together
with a separator (default: "|"). Optionally sanitizes each field using
\code{make_tree_safe()} to avoid problematic characters in tree tools.
}
